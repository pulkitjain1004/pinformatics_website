function drawTable(encodedData, encodedRow) {

    var columns = ["fname", "lname", "race", "gender", "regnum", "dob"];
    // Create the table
    var table = d3.select("#area1").append("table");
    var thead = table.append("thead");
    var tbody = table.append("tbody");

    // refer to     http://bl.ocks.org/nautat/4085017
}
    
